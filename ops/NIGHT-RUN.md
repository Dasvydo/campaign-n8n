# Night run — 2026-09-08

Autonomous work plan. **This file is the single source of truth for the run's state.** It lives in
git on purpose: the container this started in is ephemeral, so a fresh session must be able to read
this file and continue without any conversation context.

**Authorised by Dovy, 2026-09-08:**
1. Commit and push to `claude/campaign-build-status-9j9194` in each repo. Never touch `main` or any
   `campaign/*` branch.
2. Decide where evidence is clear, document the reasoning in `ops/DECISIONS.md`, keep every change a
   small revertible commit. Park anything genuinely 50/50 or outward-facing.
3. `reel-engine` is in scope.
4. Goal: close every delegable gap across all six batches, plus a morning handoff and a new-PC guide.

---

## Rules for any session continuing this run

- **Branch:** `claude/campaign-build-status-9j9194` in every repo. Never `main`, never `campaign/*`.
- **Never** run a migration, connect to Supabase, call a live API, or import anything into n8n.
- **Never** edit public marketing copy in Danish or Lithuanian.
- **Never** edit a workflow JSON by hand — they are generated by `tools/build_workflows.py`, and
  every change must re-pass `node tools/validate.mjs` and `node test/run-code-nodes.mjs`.
- Run each task's verify command before committing. If it fails, do not commit; write the failure
  into the task's Notes column and move on.
- After each task: commit, push, and update this file's status table in the same push.
- If something is ambiguous, **stop and leave it for Dovy**. A parked task is a good outcome; a
  wrong guess in a live campaign is not.

### Environment setup a fresh session needs

```bash
pip install sqlglot dnspython pytest Pillow
cd /home/user/reel-engine && pip install -r requirements.txt   # if working batch D
```

`reel-engine` may not be attached to a fresh session. Attach it with the `add_repo` tool
(`owner: Dasvydo, repo: reel-engine`), clone it, then check out
`claude/campaign-build-status-9j9194`.

---

## Status

Legend: ✅ done and pushed · 🔄 in progress · ⏸ parked for Dovy · ⬜ not started

| ID | Task | Repo | Status | Verify |
|---|---|---|---|---|
| N-01 | Add the missing `.gitignore` | campaign-n8n | ✅ `a5cbcd5` | `git check-ignore` over tracked paths: 0 hits |
| N-02 | `requirements.txt` (sqlglot) | campaign-ledger | ✅ `850050b` | `python3 tests/run_local_proof.py` → 46/0 |
| N-03 | `requirements.txt` (Pillow, pytest) | ad-engine | ✅ `8906411` | `python3 -m pytest tests/ -q` → 89 passed |
| N-04 | Move all six repos onto their real batch branch and push | all | ✅ | all six branches on GitHub |
| N-05 | Correct `STATUS.md` — the first audit read `main` in three repos | ops | ✅ | see `ops/STATUS.md` §0 |
| N-06 | New-PC setup guide | ops | ✅ | `ops/NEW-PC-SETUP.md` |
| N-07 | Record autonomous decisions and parked items | ops | ✅ | `ops/DECISIONS.md` |
| N-08 | This plan | ops | ✅ | this file |
| N-09 | Reconstruct `campaign-specs/00-START-HERE.md` from the implementations | campaign-n8n | ⬜ | every field traceable to a cited file:line |
| N-10 | Make C's shim-only tests skip cleanly when the real ledger is importable | outreach-engine | ⬜ | suite green both with and without `PYTHONPATH` |
| N-11 | Document the `PYTHONPATH` ledger wiring in each engine README | C, D, E | ⬜ | rule stated in all three READMEs |
| N-12 | Morning handoff | ops | ⬜ | `ops/HANDOFF.md` |
| P-1 | 9x vs ~5x ROI figure | site + n8n | ⏸ Dovy | see `ops/DECISIONS.md` P-1 |
| P-2 | Nurture opt-in: `mailto:` → POST | site + n8n | ⏸ Dovy | see `ops/DECISIONS.md` P-2 |
| P-3 | Where `sql/004_consent.sql` should live | ledger + n8n | ⏸ Dovy | see `ops/DECISIONS.md` P-3 |

---

## Verified baseline — what "green" means

Re-establish this before and after any change. Every number was measured on 2026-09-08.

| Repo | Command | Expected |
|---|---|---|
| campaign-ledger | `python3 tests/run_local_proof.py` | 46 passed, 0 failed |
| outreach-engine | `python3 -m pytest tests/ -q` *(no PYTHONPATH)* | 138 passed, 0 failed |
| outreach-engine | `PYTHONPATH=…/campaign-ledger/src python3 -m pytest tests/test_ledger_contract.py -q` | 28 passed |
| reel-engine | `python3 -m pytest -q -m "not slow and not network"` | 441 passed |
| reel-engine | `PYTHONPATH=… python3 -m pytest tests/test_ledger_contract.py -q` | 8 passed |
| ad-engine | `python3 -m pytest tests/ -q` | 89 passed |
| ad-engine | `PYTHONPATH=… python3 -m pytest tests/test_ad_stats.py -q` | 13 passed |
| campaign-n8n | `node tools/validate.mjs` | 6 files, 157 nodes, 0 errors |
| campaign-n8n | `node tools/validate-selftest.mjs` | 16 passed, 0 failed |
| campaign-n8n | `node test/run-code-nodes.mjs` | 135 passed, 0 failed |

---

## Things established tonight that were previously unknown

These are the findings that changed the picture. A continuing session should not re-derive them.

1. **All six batches finished.** Every repo has `RUN-REPORT.md`, `AUDIT.md` and `BLOCKED.md` on its
   `campaign/*` branch. An earlier audit read `main` in three repos and concluded otherwise.

2. **The cross-batch ledger seam works, and this is the first time it was proven.** Batches C, D and
   E each try `import campaign_db` and fall back to a local JSONL shim. Putting
   `campaign-ledger/src` on `PYTHONPATH` flips all three to the real client, and each engine's
   `test_ledger_contract.py` passes against it (28 / 8 / 13). Every table in `campaign` has a
   writer — the earlier claim that seven of ten had none is withdrawn.

3. **C's full suite must run WITHOUT the ledger on the path.** `engine/ledger.py:725`'s
   `use_shim_dir()` deliberately raises `RuntimeError: use_shim_dir() must never run against the
   real ledger`. With `PYTHONPATH` set the suite reports 3 failed + 9 errors — that is the guard
   working, not a defect. The guard should stay; only the reporting is worth improving (N-10).

4. **The reply-sentiment taxonomy is consistent everywhere.** All six canonical values
   (`interested`, `not_now`, `not_a_fit`, `referred`, `objection`, `unsubscribe`) agree across B, C
   and F. Every appearance of a retired value (`hot_pain`, `curious`, `endorse`, `unrelated`,
   `ineligible`) is either changelog prose or a **negative assertion proving it is rejected** —
   `outreach-engine/tests/test_ledger_contract.py:471`,
   `campaign-n8n/test/run-code-nodes.mjs:600`, `campaign-n8n/tools/validate-selftest.mjs:51`.

5. **The price correction landed everywhere.** All three copies of `docs/ICP-BRIEF.md` on the batch
   branches carry $89/seat + $500 setup and explicitly withdraw the $49/$99/$750 figures. Only the
   scaffold `main` branches still show the old numbers.

6. **`WF-C6`'s script paths are correct.** It invokes `report/pull_ad_stats.py` in `ad-engine` and
   `src/friday_brief.py` in `campaign-ledger` — both exactly where those batches put them.

7. **Batch F's citations are accurate.** `reel-engine/engine/ledger.py` with `CONTENT_COLUMNS` does
   exist, on `campaign/d-reels`. So does everything F quoted from `publish.py`.

---

## What no amount of night running can do

These need Dovy and an account. They are listed in execution order in `ops/NEW-PC-SETUP.md` §4.

- Expose schema `campaign` to PostgREST *(one minute; unblocks every ledger write in the campaign)*
- Run migrations 001 → 002 → 003
- Import the six workflows into n8n and create their seven credentials
- The five Vercel environment variables, the demo video, the logo, the footer company block
- A Meta Business account, a booking link, PostHog keys
- A native Danish and Lithuanian copy review
- The Danish marketing-law confirmation that gates all Danish email
